Question:
Find workers and their corresponding information from the table with an odd number for their worker id.

Link:
https://platform.stratascratch.com/coding/10136-find-workers-with-an-odd-number-for-worker-id?code_type=3

My solution:
select * from worker
where worker_id IN(select worker_id from worker where worker_id%2 = 1);

